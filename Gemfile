# frozen_string_literal: true

source 'https://rubygems.org'

# Specify your gem's dependencies in cleanse.gemspec
gemspec

gem 'rake', '~> 13.0'

gem 'minitest', '~> 5.0'
gem 'minitest-spec-context'

gem 'rake-compiler', '~> 0.9'

gem 'hitimes'
gem 'htmlfilter'
gem 'loofah'
gem 'sanitize'

group :test do
  gem 'nokogumbo', '~> 2.0'
end
